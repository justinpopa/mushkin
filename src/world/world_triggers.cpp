/**
 * world_triggers.cpp - Trigger and Alias Management
 *
 * Trigger/alias registry operations have been moved to AutomationRegistry.
 * WorldDocument forwards all calls to m_automationRegistry via inline wrappers
 * declared in world_document.h.
 *
 * This file is retained as a placeholder to preserve the build target entry.
 */

// No implementations here — all moved to automation_registry.cpp.
// WorldDocument forwarding wrappers are inline in world_document.h.
